[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    slow: tests that run real Whisper/ffmpeg (deselect with '-m "not slow"')
